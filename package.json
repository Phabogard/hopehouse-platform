{
  "name": "hopehouse-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Hope House ERP platform MVP backend and design foundation.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/server.js",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
